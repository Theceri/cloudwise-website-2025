# A few dependencies (e.g. @carlos8a/react-whatsapp-floating-button) still
# declare a React 18 peer, but the app runs on React 19. They work fine at
# runtime; this lets `npm install` resolve without --legacy-peer-deps flags
# (needed for CI/Vercel which run a strict install).
legacy-peer-deps=true
