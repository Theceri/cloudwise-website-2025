export const metadata = {
  title: 'Portfolio — Selected Work',
  description:
    'Explore Cloudwise case studies: AI platforms, mobile apps, enterprise web portals and marketplaces that delivered measurable results.',
  alternates: { canonical: '/portfolio' },
};

export default function PortfolioLayout({ children }) {
  return children;
}
